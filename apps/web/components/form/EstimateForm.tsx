import {Estimate} from "../../../../libs/SDK";

export default function EstimateForm({
                                         estimate,
                                         onSubmit,
                                     }:
                                         {
                                             estimate?: Estimate | null,
                                             onSubmit: (estimate: Estimate) => void,
                                         }
) {
    // Used to create a form for Estimates

    return (
        <></>
    )
}
